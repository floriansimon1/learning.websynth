/** @file Redux actions */

const toggleNote = (instrument, position) => ({
    type: Symbol()
});

/**
 * Redux actions
 *
 * @namespace actions
 * @memberof module:client.redux
 */
module.exports = { toggleNote };
